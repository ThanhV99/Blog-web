---
import server_url from "@config/serverUrl.json"

export async function fetchApiSinglePost(id: number) {
  const response = await fetch(`${server_url.server}${server_url.listPosts}${id}`);
  const data = await response.json();
  return data;
}
---

export default fetchApiSinglePost;