---
import server_url from "@config/serverUrl.json"

export async function fetchApiListPosts() {
  const response = await fetch(`${server_url.server}${server_url.listPosts}`);
  const data = await response.json();
  return data;
}
---

export default fetchApiListPosts;